json.extract! elevator, :id, :serial_number, :model, :elevatory_type, :status, :start_date, :last_check, :certificate, :information, :notes, :column_id, :created_at, :updated_at
json.url elevator_url(elevator, format: :json)
