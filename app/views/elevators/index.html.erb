<p id="notice"><%= notice %></p>

<h1>Elevators</h1>

<table>
  <thead>
    <tr>
      <th>Serial number</th>
      <th>Model</th>
      <th>Elevatory type</th>
      <th>Status</th>
      <th>Start date</th>
      <th>Last check</th>
      <th>Certificate</th>
      <th>Information</th>
      <th>Notes</th>
      <th>Column</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @elevators.each do |elevator| %>
      <tr>
        <td><%= elevator.serial_number %></td>
        <td><%= elevator.model %></td>
        <td><%= elevator.elevatory_type %></td>
        <td><%= elevator.status %></td>
        <td><%= elevator.start_date %></td>
        <td><%= elevator.last_check %></td>
        <td><%= elevator.certificate %></td>
        <td><%= elevator.information %></td>
        <td><%= elevator.notes %></td>
        <td><%= elevator.column %></td>
        <td><%= link_to 'Show', elevator %></td>
        <td><%= link_to 'Edit', edit_elevator_path(elevator) %></td>
        <td><%= link_to 'Destroy', elevator, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Elevator', new_elevator_path %>
